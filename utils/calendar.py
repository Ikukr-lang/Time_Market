from datetime import datetime, timedelta
from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton

async def generate_calendar(specialist_id: int, month_offset: int = 0):
    # здесь логика показа дней на 60 дней вперёд + проверка занятости
    # возвращает InlineKeyboardMarkup с датами
    pass  # я могу прислать полную версию в следующем сообщении
